{
  "name": "gestion-horarios-universitarios",
  "private": true,
  "version": "0.1.0",
  "description": "Sistema de generación de horarios universitarios con motor CSP sobre Firebase",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "concurrently \"npm -w backend run dev\" \"npm -w frontend run dev\"",
    "lint": "npm -w frontend run lint && npm -w backend run lint",
    "typecheck": "npm -w frontend run typecheck && npm -w backend run build",
    "test": "npm -w frontend run test",
    "format": "prettier --write \"**/*.{ts,tsx,json,md,yml,yaml}\" --ignore-path .gitignore",
    "format:check": "prettier --check \"**/*.{ts,tsx,json,md,yml,yaml}\" --ignore-path .gitignore"
  },
  "workspaces": [
    "frontend",
    "backend"
  ],
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.4.2"
  }
}
